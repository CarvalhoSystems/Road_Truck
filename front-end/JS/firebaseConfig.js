// ⚠️ DEPRECATED: Este arquivo foi consolidado em config.js
// Use: import { auth, db } from "./config.js"

// Para manter compatibilidade, redireciona as importações:
import { auth, db } from "./config.js";
export { auth, db };
